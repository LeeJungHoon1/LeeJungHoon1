git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/LYJ0307/front.git
git push -u origin main


업로드 순서
터미널에서 git bash로 들어간 뒤 시작
1. git add .
2. git commit -m "day1 second"
파일 올릴 때 어떤 메시지와 올릴 것인지, 언제 올릴 것인지 구분하는 용도로 적음
3. git push -u origin main